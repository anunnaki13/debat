import { SettingsScreen } from "@/components/settings/SettingsScreen";

export const metadata = {
  title: "Settings | Republik Argumen",
};

export default function SettingsPage() {
  return <SettingsScreen />;
}
